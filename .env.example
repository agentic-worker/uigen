# Anthropic API key — enables the real AI provider.
# If unset, the app falls back to a mock provider.
ANTHROPIC_API_KEY=

# Secret used to sign auth JWTs. Use a long, random string in production.
JWT_SECRET=development-secret-key

# Prisma database connection (SQLite by default).
DATABASE_URL="file:./dev.db"
