Your goal is to update any vulnerable dependencies

Do the following:

1. Run `npm audit` to find vulnerable installed packages in this project
2. Run `npm audit fix` to apply updates.
Update the package to the latest secure version using 'npm update <package-name>' or by modifying the package.json file and running 'npm install'.
3. Run tests and verify the updates didn't break anything
After updating the packages, run 'npm audit' again to ensure that all vulnerabilities have been resolved.
4. If any vulnerabilities remain, consider alternative packages or solutions to mitigate the risk.
5. Document the changes made, including the packages updated and the versions they were updated to, in a changelog or project documentation.
